<div class="container-fluid">
	<div class="row">
		<!-- トップ画像 -->
		<div class="home_top_container">
			<div class="home_top_visual">
				<h2 class="home_top_heading">ようこそHist-Postへ！</h2>
				<p class="home_top_paragraph">授業では教えてもらえない<br>たくさんのエピソードが歴史には<br>あります。<br>みなさんで教えあいながら<br>楽しく学んでみませんか？</p>
			</div>
		</div>

		<!-- コンテンツ左側 主に投稿表示機能-->
		<div class="col-md-8">
			<div class="home_left_container">
				<table class="table table-hover">
					<thead>
						<tr>
							<th>投稿者画像</th>
							<th>投稿者</th>
							<th>タイトル</th>
							<th>投稿画像</th>
							<th>概要</th>
						</tr>
					</thead>
					<tbody>
						<% @posts.each do |post| %>
							<tr>
								<td><%= attachment_image_tag post.user, :user_image, :fill, 40, 40, fallback: "no_image_small.jpg", alt:"User image" %></td>
								<td><%= link_to post.user.user_name, user_path(post.user.id) %></td>
								<td><%= link_to post.post_title, post_path(post) %></td>
								<td><%= attachment_image_tag post, :post_image, :fill, 150, 150 %></td>
								<td><%= post.top_explanation %></td>
							</tr>
						<% end %>
					</tbody>
				</table>
				<%= paginate @posts, class: "paginate" %>
				<%= page_entries_info(@posts) %>
			</div>
		</div>

		<!-- コンテンツ右側　主に検索機能 -->
		<div class="col-md-4">
			<div class="home_right_container">
				<div class="home_right_btn">
					<% if user_signed_in? %>
						<%= link_to '新しく投稿する', new_post_path(current_user), class: "btn_new_post" %>
					<% else %>
						<%= link_to 'ログインすると投稿ができます！', new_user_session_path, class: "btn_new_post" %>
					<% end %>
				</div>
			</div>
		</div>


	</div>
</div>
